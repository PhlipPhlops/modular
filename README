## Getting Started
After cloning the repo, you'll need to set up the frontend and backend individually.

## Frontend
In the frontend-react folder, run `npm install` to collect all packages.

# Start the frontend development server
In `frontend-react/`, run `npm start` to fire up webpack and start the development server